'use client';

// Japanese minimal aesthetic - no shooting stars needed
// Component kept for compatibility but renders nothing
export default function ShootingStars({ count = 3 }: { count?: number }) {
  return null;
}

